#include <iostream>
using namespace std;
int main(){
    long long  N,a,b;
    cin >> N;
    for(int i = 0; i < N; i++){
         cin >> a >> b;
        if(a >= b)
           cout << "MMM BRAINS\n";
        else
        cout << "NO BRAINS\n";
        }
    }
   